# Whisper Model Configuration
# 使用するWhisperモデルを指定します
#
# 利用可能なモデル:
#   - tiny: 最も高速だが精度は低め（39M parameters）
#   - base: 高速で軽量（74M parameters）
#   - small: バランス型（244M parameters）【デフォルト】
#   - medium: 高精度だが重い（769M parameters）
#   - large: 最高精度だが非常に重い（1550M parameters）
#   - large-v2: large の改良版
#   - large-v3: large の最新版
#
# 注意:
#   - 設定しない場合、または無効な値の場合は "small" が使用されます
#   - より大きいモデルほど精度が高いですが、処理時間も長くなります
#   - GPUがない環境では medium 以下を推奨します
#
WHISPER_MODEL=small

# Logging Configuration
# ログレベルを指定します
#
# 利用可能なレベル:
#   - DEBUG: 詳細なデバッグ情報を表示（開発時に有用）
#   - INFO: 一般的な情報を表示（通常運用時に推奨）【デフォルト】
#   - WARNING: 警告メッセージのみ表示
#   - ERROR: エラーメッセージのみ表示
#   - CRITICAL: 致命的なエラーのみ表示
#
# 注意:
#   - DEBUGは音声処理の詳細ログを出力するため、大量のログが出力されます
#   - 本番環境ではINFO以上を推奨します
#
LOG_LEVEL=INFO
